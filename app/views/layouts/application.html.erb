<!DOCTYPE html>
<html>
  <head>
    <title>HannaSolutionsChallenge</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% flash.each do |type, msg| %>
      <div style="font-weight: 600; color: red; display:flex; justify-content: center; margin: 1rem;">
        <%= msg %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
